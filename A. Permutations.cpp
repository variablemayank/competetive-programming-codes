	#include<bits/stdc++.h>
	using namespace std;
	
	const int maxi = 2000005;
	int arr[maxi];
	int brr[maxi];
	int soln[maxi];
	
	int main()
	{
		int n;
		cin>>n;
		for(int i=0;i<n;i++) cin>>arr[i];
		for(int i=0;i<n;i++) arr[i]--;
		
		for(int i=0;i<n;i++) cin>>brr[i];
		for(int j=0;j<n;j++) brr[j]--;
		
		for(int i=0;i<n;i++)
		{
			cout<<arr[i]<<" ";
		}
		cout<<endl;
		for(int j=0;j<n;j++)
		{
			cout<<brr[j]<<" ";
		}
		
		cout<<endl;
		for(int i=0;i<n;i++)
		{
		  cout<<" b of i is "<<b[i]<<endl;
		  cout<<soln[b[i]] =
		}
 	}
